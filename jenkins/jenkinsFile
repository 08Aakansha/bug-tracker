pipeline {
    agent any

    // This was the missing wrapper block
    stages {
        
        stage ('Test Backend') {
            agent {
                docker {
                    image 'snakee/golang-junit:1.21'
                    reuseNode true
                    alwaysPull true
                }
            }
                
            steps {
                dir('bugtracker-backend') {
                    sh 'go test -v ./... '
                }
            }
        }
        
    } // End of stages
}